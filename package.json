{
  "name": "mandarin-game",
  "version": "1.0.0",
  "private": true,
  "description": "MandarinGame - 母语纠音游戏化学习应用",
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "format": "prettier --write \"**/*.{ts,tsx,vue,js,json}\""
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  }
}
